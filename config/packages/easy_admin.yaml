# config/packages/easy_admin.yaml
easy_admin:
    entities:
        Livre:
            # ...
            list:
                fields:
                    - { property: 'image', type: 'image', base_path: '%app.path.book_images%' }
            # ...
            show:
                fields:
                    - { property: 'image', type: 'image', base_path: '%app.path.book_images%' }